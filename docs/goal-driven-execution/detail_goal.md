# /goal Detail Contract

> Auto-generated by Agent Skill MCP instrumentation for ChurchThrive.

## Required Read Order

1. Read `ai_agent.md`.
2. Read this file.
3. Read `docs/index/capability-catalog.md` + `docs/guides/capability-first-execution.md` (reuse built capabilities first).
4. Read `docs/Task_Work/README.md` or `docs/Task_Work/INDEX.md` when present.
5. If a task folder already exists, read its `goal.md`, `detail_goal.md`, and `analysis.md` before planning.

## Task Folder Contract

For a project-sized request, create or update:

```text
docs/Task_Work/{YYYY-MM-DD}_{slug}/
  goal.md         # /goal input (<=4000 chars) + stable final objective
  detail_goal.md  # long requirements that resolve goal.md
  analysis.md     # existing assets + capability + duplication review (MUST, before building)
  context.md
  plan.md
  progress.md
  decisions.md
  verification.md
  subgoals/
```

`goal.md` is the stable objective and success criteria (max ~4000 chars). Do not rewrite it unless the user explicitly changes the goal.

## Scaffold Is Only the First Step

Creating the scaffold (the files above) does NOT complete the task. Scaffolding is step 0.
Never report a task complete just because the folder/templates exist. Real implementation
and verified working behavior are required for completion.

## Step Gate

Each step must record capability-first, duplication, implementability, completeness, regression, and documentation checks in `verification.md`.

## Completion Contract (completion is not file existence)

A task is complete only when ALL of the following hold:

1. `goal.md` success criteria are met by a real, working implementation (not placeholders).
2. `verification.md` gates are filled with concrete evidence (commands/results), not left "pending".
3. Tests/build/lint were run where applicable; results recorded (or a stated reason if not).
4. Durable facts distilled to `docs/index/` and procedures to `docs/guides/` when applicable.
5. `docs/Task_Work/{id}/.distilled` written with the durable paths that were updated.

If only the scaffold exists with unfilled placeholders, the task is NOT complete (report partial/blocked).
